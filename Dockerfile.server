FROM node:20-alpine

WORKDIR /app

# for server dependencies and installing them
COPY server/package*.json ./server/
RUN cd server && npm install --production

# Copy server source
COPY server/ ./server/

# Create a data directory for SQLite
RUN mkdir -p /app/server/data

EXPOSE 3001
CMD ["node", "server/index.js"]
